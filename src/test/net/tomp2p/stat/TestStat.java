package net.tomp2p.stat;


public class TestStat
{
	/*
	 * @Test public void testNodesEstimation1() throws UnknownHostException {
	 * Number160 id1 = new Number160("1"); Number160 id2 = new Number160("3");
	 * Number160 id3 = new Number160("5"); Number160 id4 = new Number160("7");
	 * Number160 id5 = new Number160("9"); Number160 id6 = new Number160("11");
	 * Number160 id7 = new Number160("13"); Number160 id8 = new Number160("15");
	 * PeerMapKadImpl kadRouting = new PeerMapKadImpl(id4); PeerAddress
	 * remoteNode1 = Utils2.createNodeAddress(id1); kadRouting.add(remoteNode1,
	 * true); // PeerAddress remoteNode2 = Utils2.createNodeAddress(id2);
	 * kadRouting.add(remoteNode2, true); // PeerAddress remoteNode3 =
	 * Utils2.createNodeAddress(id3); kadRouting.add(remoteNode3, true); // //
	 * NodeAddress remoteNode4=Utils2.createNodeAddress(); //
	 * remoteNode4.setID(id4); // kadRouting.add(remoteNode4); // PeerAddress
	 * remoteNode5 = Utils2.createNodeAddress(id5); kadRouting.add(remoteNode5,
	 * true); // PeerAddress remoteNode6 = Utils2.createNodeAddress(id6);
	 * kadRouting.add(remoteNode6, true); // PeerAddress remoteNode7 =
	 * Utils2.createNodeAddress(id7); kadRouting.add(remoteNode7, true); //
	 * PeerAddress remoteNode8 = Utils2.createNodeAddress(id8);
	 * kadRouting.add(remoteNode8, true); // TODO: find equivalent //
	 * System.err.println("We have 8 nodes, we esimate " + //
	 * kadRouting.expectedNumberOfNodes()); }
	 * 
	 * @Test public void testNodesEstimation2() throws UnknownHostException {
	 * int testNodes = 1000; for (int i = 2; i < testNodes; i++) { Number160 id
	 * = Utils.makeSHAHash("bla"); PeerMapKadImpl kadRouting = new
	 * PeerMapKadImpl(id); for (int j = 0; j < i; j++) { Number160 id2 =
	 * Utils.makeSHAHash("" + (j + 1)); // id2=Utils.reduceBits(id2,16);
	 * PeerAddress remoteNode = Utils2.createNodeAddress(id2);
	 * kadRouting.add(remoteNode, true); } // TODO: find equivalent //
	 * System.err.println("We have " + i + " nodes, we esimate " // +
	 * kadRouting.expectedNumberOfNodes()); } }
	 */
}
